import styled from 'styled-components';

const CustomButton = styled.button`
    background: yellow;
    color: #000;
    border-radius: 4px;
    cursor: pointer;
    font-size: 1rem;
    font-weight: 300;
    padding: 9px 36px;
`;

export default CustomButton;
